---
- name: Enable vdirsyncer service timer
  systemd: daemon_reload=yes user=yes name=vdirsyncer.timer enabled=yes
  become: yes                                                                                                                                                                                                    
  become_method: machinectl
  become_user: "{{ user.name }}"
  tags:
    - cli
    - pim
    - systemd
